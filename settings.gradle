pluginManagement {
    repositories {
        google()
        maven { url "https://chaquo.com/maven" }
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)

    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "jualanonline"
include ':app'
